import './Button.css';

function Button({ color, children, onClick }) {
  const class
  return (
    <button style={style} onClick={onClick}>
      {children}
    </button>
  );
}

export default Button;
